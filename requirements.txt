# ConfigerBot - 可扩展 AI Agent 框架
# 核心依赖（纯 Python 标准库实现，无需额外安装）

# 可选依赖（用于增强功能）：
# openai>=1.0.0          # 对接 OpenAI 兼容 LLM
# httpx>=0.25.0          # 异步 HTTP 客户端（MCP 远程连接）
# pydantic>=2.0.0        # 数据模型验证
# pyyaml>=6.0            # YAML 配置解析