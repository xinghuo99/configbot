对skills机制，增加更强的功能：
1、skill自动创建和安装：通过聊天指令，比如帮我将以上操作生成一个skills，这句话，可以自动生成skill，自动安装这个skill
2、skill自动更新和进化：通过聊天指令，比如执行skill A,执行过程中，根据用户的输入，进行了一些调整，根据新调整的内容，自动优化skill A并更新进化skill A，然后自动重新安装和重新加载。
3、skill卸载：通过聊天指令，比如 卸载skill B，可以将skill B卸载掉。

请增加以上skill自我管理和自我进化的功能。


main函数中是写死的调用方式，期望是用自然语言的方式将用户输入的信息作为一个参数传入给agent，由agent自主选择工具和调用工具。请实现该功能，并提供一个统一的聊天函入口，要求是多轮对话的方式。

请按照以上方式优化。


这个项目用vscode打开，项目可以运行，但是vscode总是不识别main.py文件的28行代码到32行代码的头文件，显示波浪线，且报告警 could not be resolved Pylance的告警。重新加载项目也不解决不了。

请深度分析根本原因，并修复这个问题。

1、重新整理下项目目录结构：
①将所有文件和文件夹放到configbot文件夹下。
②将main.py拷贝到configbot文件夹同级的目录下。
③期望在configbot文件夹下运行项目，可以运行通过。同时在configbot文件夹的父级目录下运行main.py也可以运行通过。
2、将所有需要input的地方，统一修改成传入字符串参数的方式，统一在函数调用最外层传入需要输入的信息，请优化。
3、log.py中新增一个函数：请实现该函数，该函数有两个功能，一个是将信息print出来，一个是将信息保存到日志中。实现后，将项目中所有使用print的地方统一替换成这个函数。
4、如何配置LLM启用AI推理，包括api key、model name、model url ，在哪里配置？如何配置？
